{% extends 'base.html.twig' %}

{% block title %}Hello ClientAgendaController!{% endblock %}

{% block body %}

    <h2>Welcome {{ client.fullName }}. This is your planning for today.</h2>

    {% for training in trainings %}

        <h3><a href="{{ path('app_client_training_detail', {'id': training.id}) }}">{{ training.title }}</a></h3>

    {% endfor %}

    <div><a href="#">Check your full calendar</a></div>

{% endblock %}
